tags: [[Loading Source JSON]]
title: File Picker
type: text/vnd.tiddlywiki

The ''Choose File'' button opens a native file picker.  Any `.json` or `.txt` file is read via the browser's `FileReader.readAsText` and dropped into the source area.  Pure-text formats only -- no extraction from `.zip` or other archives.
